import 'package:flutter/material.dart';

class CompleteGoogleAccountScreen extends StatelessWidget {
  const CompleteGoogleAccountScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
